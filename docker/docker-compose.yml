version: "3.9"

services:
  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: totsource_frontend

    restart: unless-stopped

    ports:
      - "4000:80"
